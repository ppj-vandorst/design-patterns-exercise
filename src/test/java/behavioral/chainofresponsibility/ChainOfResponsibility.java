package behavioral.chainofresponsibility;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

public class ChainOfResponsibility {

    @Test
    public void itShouldAllowObjectsToObserveTheInventoryService() {
        // var textParseService = new TextParseService();
        // var htmlParseService = new HtmlParseService();
        // var parseService = new JsonParseService();

        // parseService.setNext(htmlParseService);
        // htmlParseService.setNext(textParseService);

        // var result1 = parseService.handle("<html><body>test<body></html>");
        // var result2 = parseService.handle("{ \"message\": \"test\" }");
        // var result3 = parseService.handle("test");

        // assertThat(result1).isEqualTo("test");
        // assertThat(result1).isEqualTo("test");
        // assertThat(result1).isEqualTo("test");
    }
}
